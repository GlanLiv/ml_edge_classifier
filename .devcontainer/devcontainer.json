{
  "name": "Edge Classifier Environment",
  "image": "mcr.microsoft.com/devcontainers/python:3.10",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y git-lfs && git lfs install && pip install -r requirements.txt",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  },
  "features": {}
}

