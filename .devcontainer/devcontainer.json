{
  "name": "Edge Classifier Environment",
  "image": "mcr.microsoft.com/devcontainers/python:3.10",
  "postCreateCommand": "apt-get update && apt-get install -y git-lfs libgl1 && git lfs install && pip install -r requirements.txt",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  },
  "features": {}
}
